<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Thêm permission cho Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Main Activity - exported=true vì có intent-filter LAUNCHER -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Search Activity - exported=false vì chỉ dùng nội bộ -->
        <activity
            android:name=".SearchActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.Dialog" />

        <!-- Weather Map Activity - exported=false vì chỉ dùng nội bộ -->
        <activity
            android:name=".WeatherMapActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar" />

        <!-- Favorite Cities Activity - exported=false vì chỉ dùng nội bộ -->
        <activity
            android:name=".FavoriteCitiesActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar" />

        <!-- Clothing Suggestion Activity - exported=false vì chỉ dùng nội bộ -->
        <activity
            android:name=".ClothingSuggestionActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.Dialog" />

        <!-- Chart Activity - exported=false vì chỉ dùng nội bộ -->
        <activity
            android:name=".ChartActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar" />

        <!-- Air Quality Activity - exported=false vì chỉ dùng nội bộ -->
        <activity
            android:name=".AirQualityActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar" />

        <!-- XÓA 2 ACTIVITY KHÔNG TỒN TẠI -->
        <!--
        <activity
            android:name=".SimpleChartActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar" />

        <activity
            android:name=".SimpleAirQualityActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar" />
        -->

        <!-- Weather Notification Service - exported=false vì chỉ dùng nội bộ -->
        <service
            android:name=".service.WeatherNotificationService"
            android:exported="false" />

        <!-- XÓA ClothingSuggestionService VÌ NÓ KHÔNG PHẢI SERVICE -->
        <!--
        <service
            android:name=".service.ClothingSuggestionService"
            android:exported="false" />
        -->

        <!-- Boot Receiver - exported=true vì cần nhận BOOT_COMPLETED từ hệ thống -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

    </application>

</manifest>